{
  "name": "@obpub/cli",
  "version": "0.0.0",
  "private": true,
  "description": "CLI (obpub dev/build/audit/status) for Obsidian-Publish-OSS.",
  "type": "module",
  "bin": {
    "obpub": "./src/bin.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "obpub": "node --experimental-strip-types ./src/bin.ts"
  },
  "dependencies": {
    "@obpub/core": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
